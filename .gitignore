__pycache__/
*.py[cod]
*.pyo
*.pyd

.venv/
env/
venv/

node_modules/
dist/
build/

# Git worktrees (local dev only)
.worktrees/
worktrees/

.DS_Store
Thumbs.db

.env
.env.*

# Local AI/CLI tooling

# Keep `.opencode/` and `.gemini/` in-repo (agents need them).
# But never commit dependency folders or embedded git metadata.
.opencode/node_modules/
.opencode/**/.git/
